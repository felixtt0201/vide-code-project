# 開發任務清單

## 概述

根據 design.md，分為 4 個主要任務，每個任務獨立可開發、驗證與提交。

---

## Task 1: HTML 結構與基礎頁面

**目標**：建立 `docs/index.html`，包含所有頁面標記與模擬資料

**範圍**：
- 單一 HTML 檔案
- 包含首頁總覽、事項列表、詳細彈窗的完整標記
- 嵌入模擬資料（10-12 筆任務）
- 無 CSS、無 JavaScript（純結構）

**輸出**：
```
docs/index.html
  ├── Header（導航、標題）
  ├── Main
  │   ├── #overview 區域（首頁總覽）
  │   │   ├── 專案資訊卡片
  │   │   ├── 進度展示區
  │   │   ├── 狀態統計
  │   │   └── 即將到期事項清單
  │   │
  │   ├── #list 區域（事項列表）
  │   │   ├── 篩選按鈕組
  │   │   └── 事項表格
  │   │
  │   └── .modal（詳細彈窗，預設隱藏）
  │       └── 詳細資訊表單
  │
  └── Footer（可選）
```

**模擬資料需求**：
- 1 個專案（名稱、建立日期、目標日期、進度 65%）
- 12 筆追蹤事項：
  - 3 筆待處理（優先級分佈：高、中、低各 1）
  - 3 筆進行中（優先級分佈：高、中、低各 1）
  - 2 筆已完成
  - 其他混合優先級
  - 至少 1-2 筆逾期或即將到期（未來 3 天內）

**驗收條件**：
- [x] HTML 檔案結構清晰，使用語義化標籤
- [x] 所有必需資訊欄位都存在（事項名稱、負責人、優先級、狀態、期限、備註）
- [x] 模擬資料嵌入 `<script type="application/json">` 或直接在 JS 區塊
- [x] 所有資源使用相對路徑（`./styles.css`、`./app.js`）
- [x] 頁面能在瀏覽器打開（無 JavaScript 錯誤）
- [x] 所有檢視區域都存在，預設首頁總覽可見

**預估工作量**：30 分鐘

---

## Task 2: CSS 版面與響應式設計

**目標**：建立 `docs/styles.css`，實現完整版面與響應式設計

**範圍**：
- 單一 CSS 檔案
- Header、主區域、Footer 佈局
- 卡片式元件樣式
- 表格樣式
- 彈窗樣式
- 三個媒體查詢斷點（手機、平板、桌面）
- 色彩、字體、間距定義

**輸出**：
```
docs/styles.css
  ├── 全局樣式（色彩變數、字體、重置）
  ├── Header 樣式
  ├── 卡片式元件（.card）
  ├── 表格樣式（.table）
  ├── 彈窗樣式（.modal、.modal-overlay）
  ├── 按鈕樣式（.btn）
  ├── 進度條樣式（.progress-bar）
  ├── 時程標籤樣式（.tag、.tag-overdue、.tag-urgent）
  │
  └── 媒體查詢
      ├── @media (max-width: 767px)   ← 手機版
      ├── @media (768px to 1023px)    ← 平板版
      └── @media (1024px+)            ← 桌面版
```

**設計規格**：
- 色彩方案（design.md 中定義）
- 字體：系統預設字體堆棧
- 間距：桌面 20px、平板 15px、手機 10px
- 響應式斷點：768px、1024px

**驗收條件**：
- [ ] 所有頁面區域都有適當樣式
- [ ] 卡片、表格、按鈕等元件視覺清晰
- [ ] 色彩方案一致（高優先級紅、中橙、低綠等）
- [ ] 進度條正確顯示（65% 進度）
- [ ] 時程標籤色彩正確（紅=逾期、黃=即將到期、綠=完成）
- [ ] 三個媒體查詢都有測試（手機 375px、平板 768px、桌面 1200px）
- [ ] 彈窗背景覆蓋層正確（半透明、遮擋主區域）
- [ ] 無水平捲軸（響應式寬度正確）
- [ ] 字體大小在各尺寸都清楚可讀

**預估工作量**：45 分鐘

---

## Task 3: 資料、計算與圖表

**目標**：建立 `docs/app.js`，實現資料結構、計算邏輯與簡單圖表

**範圍**：
- 全域變數：專案資訊、追蹤事項陣列
- 計算函數（狀態統計、優先級統計、時程判斷）
- SVG 圖表生成（進度條、狀態分佈、優先級分佈）
- 無互動邏輯（只計算與渲染）

**輸出**：
```
docs/app.js
  ├── 全域變數
  │   ├── const project = { ... }
  │   └── const tasks = [ ... ]
  │
  ├── 計算函數
  │   ├── getStatusCounts()
  │   ├── getPriorityCounts()
  │   ├── getTimelineStatus(dueDate, status)
  │   └── getOverdueOrUrgent()
  │
  ├── 圖表渲染函數
  │   ├── renderProgressBar(progress)
  │   ├── renderStatusChart(counts)
  │   └── renderPriorityChart(counts)
  │
  └── 初始化
      └── document ready 時渲染數據
```

**資料規格**：
- 專案進度：65%
- 12 筆追蹤事項（見 Task 1）
- 日期使用 YYYY-MM-DD 格式
- 模擬日期跨越過去、現在、未來

**圖表規格**：
- 進度條：CSS + div（無 SVG）
- 狀態分佈：原生 SVG 圓餅圖或簡單柱狀圖
- 優先級分佈：原生 SVG 柱狀圖
- 所有圖表無第三方庫（純 SVG + JavaScript）

**驗收條件**：
- [ ] 所有計算函數返回正確結果
  - [ ] `getStatusCounts()` 返回 `{pending: 3, inProgress: 3, completed: 2}` 等正確值
  - [ ] `getPriorityCounts()` 返回 `{high: X, medium: Y, low: Z}` 正確計數
  - [ ] `getTimelineStatus()` 正確識別逾期、即將到期、正常
- [ ] 進度條在頁面正確呈現（寬度 65%）
- [ ] 圖表（圓餅圖、柱狀圖）在頁面正確渲染
- [ ] SVG 圖表在各尺寸螢幕都清楚可見
- [ ] 控制台無錯誤（所有函數正常執行）
- [ ] 資料沒有硬編碼依賴（計算邏輯可適應不同資料）

**預估工作量**：40 分鐘

---

## Task 4: 互動邏輯與整合檢查

**目標**：完善 `docs/app.js` 互動邏輯，整合所有功能，進行完整驗證

**範圍**：
- 狀態篩選（點選按鈕過濾列表）
- 詳細彈窗（點選事項顯示、關閉邏輯）
- 頁面切換（首頁總覽 ↔ 事項列表）
- 時程提示色彩應用
- 頁面初始化與渲染

**輸出**：
```
docs/app.js（完整版本）
  ├── 前三個 Task 的所有內容
  │
  └── 互動函數
      ├── filterTasks(status)
      ├── renderTaskList(filteredTasks)
      ├── showModal(taskId)
      ├── hideModal()
      ├── switchView(viewName)  // 'overview' | 'list'
      ├── applyTimelineStyles()
      └── initEventListeners()
```

**互動需求**：

### 故事 3：狀態篩選
- 點選篩選按鈕（全部、待處理、進行中、已完成）
- 列表自動過濾，顯示符合狀態的事項
- 計數標籤更新（「顯示 X 筆事項」）
- 無頁面重載

### 故事 2：首頁 → 列表切換
- 首頁顯示「查看所有事項」按鈕
- 點選後顯示列表頁面，隱藏首頁
- 列表頁面顯示「返回總覽」按鈕
- 點選後回到首頁

### 故事 5：詳細彈窗
- 點選列表中任一事項
- 彈窗顯示該事項完整資訊
- 點選「關閉」按鈕或背景區域關閉彈窗
- 彈窗內容更新正確

### 故事 4：時程提示
- 逾期事項：顯示紅色標籤「逾期」
- 即將到期（< 3 天）：顯示黃色標籤「即將到期」
- 色彩應用到列表行與彈窗
- 排序（可選）：優先顯示逾期和即將到期的事項

**驗收條件**：
- [ ] 篩選功能正常
  - [ ] 點選「待處理」只顯示待處理事項
  - [ ] 點選「進行中」只顯示進行中事項
  - [ ] 點選「已完成」只顯示已完成事項
  - [ ] 點選「全部」顯示所有事項
  - [ ] 計數正確更新
- [ ] 頁面切換正常
  - [ ] 首頁 → 列表 → 首頁 無誤
  - [ ] 各頁面顯示/隱藏正確
- [ ] 詳細彈窗正常
  - [ ] 點選事項彈窗出現
  - [ ] 彈窗顯示正確事項資訊
  - [ ] 關閉按鈕與背景點擊都能關閉
  - [ ] 連續點選不同事項彈窗更新正確
- [ ] 時程提示色彩正確
  - [ ] 逾期事項標籤為紅色
  - [ ] 即將到期事項標籤為黃色
  - [ ] 已完成事項無時程標籤
- [ ] 響應式檢驗
  - [ ] 手機版（375px）功能無誤
  - [ ] 平板版（768px）功能無誤
  - [ ] 桌面版（1200px）功能無誤
- [ ] 無控制台錯誤或警告
- [ ] 所有故事驗收條件都符合（見 requirements.md）

**預估工作量**：50 分鐘

---

## 總結

| Task | 內容 | 工作量 | 順序 |
|------|------|--------|------|
| 1 | HTML 結構與資料 | 30 分 | 第一（基礎） |
| 2 | CSS 版面與響應式 | 45 分 | 第二（搭配 Task 1 驗證） |
| 3 | 資料、計算、圖表 | 40 分 | 第三（依賴 Task 1） |
| 4 | 互動邏輯與整合 | 50 分 | 第四（依賴前三個 Task） |

**總工作量**：~165 分鐘（2.75 小時）

**開發流程**：
1. Task 1 完成 & 驗收 → git commit
2. Task 2 完成 & 驗收 → git commit
3. Task 3 完成 & 驗收 → git commit
4. Task 4 完成 & 驗證全部故事 → git commit

**每個 Task 都獨立可驗證，每個 commit 都代表可用狀態。**
